<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>The Empire — Welcome</title>
  <style>
    :root { 
      --bg:#0b0e12; 
      --panel:#121624; 
      --text:#e8eaed; 
      --muted:#b6b9c2; 
      --gold:#f1c40f; 
      --shadow:rgba(0,0,0,.5); 
    }
    * { box-sizing:border-box; margin:0; padding:0; }
    body {
      background:var(--bg); color:var(--text);
      font-family: system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;
      display:flex; flex-direction:column; align-items:center;
      min-height:100vh; padding:22px;
    }
    .banner {
      max-width: 640px; width:100%;
      border-radius:12px; box-shadow:0 12px 32px var(--shadow);
    }
    .banner-caption {
      margin-top:12px;
      font-size: clamp(1rem, 2.5vw, 1.3rem);
      font-weight:700;
      color: var(--gold);
      text-align:center;
      letter-spacing:.03em;
    }
    h1 {
      margin:28px 0 10px;
      font-size: clamp(1.4rem, 3.5vw, 2rem);
      font-weight:800; text-align:center;
    }
    p.tagline {
      text-align:center; color:var(--muted); font-size:1rem;
      margin-bottom:22px;
    }
    .actions {
      display:flex; gap:12px; flex-wrap:wrap;
      justify-content:center; margin-bottom:20px;
    }
    .btn {
      padding:12px 18px; border-radius:10px; border:none;
      cursor:pointer; font-weight:700; font-size:1rem;
      box-shadow:0 8px 24px var(--shadow);
    }
    .btn-primary { background:var(--gold); color:#000; }
    .btn-dark { background:#222; color:var(--text); }
    footer { margin-top:auto; color:var(--muted); text-align:center; font-size:.9rem; }
  </style>
<link rel="stylesheet" href="/assets/css/empire-style.css">
<script src="/assets/js/empire-frame.js" defer></script>
<link rel="stylesheet" href="/assets/css/empire-style.css">
<script src="/assets/js/empire-frame.js" defer></script>
</head>
<body>
  <header>
         class="banner"/>
    <div class="banner-caption">⚔️ The Empire Affiliate System ⚔️</div>
  </header>

  <h1>Welcome to THE EMPIRE</h1>
  <p class="tagline">Automated. Intelligent. Profitable. 24/7.</p>

  <div class="actions">
    <button class="btn btn-primary">Enter Command</button>
    <button class="btn btn-dark">View Dashboard</button>
    <button class="btn btn-dark">Join HQ</button>
  </div>

  <footer>Commanding the Future of Affiliate Marketing</footer>
</body>
</html>
