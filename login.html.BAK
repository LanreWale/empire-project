<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>The Empire — Commander Login</title>
  <style>
    :root {
      --bg:#0b0e12; 
      --panel:#121624; 
      --text:#e8eaed; 
      --muted:#b6b9c2; 
      --gold:#f1c40f; 
      --shadow:rgba(0,0,0,.5);
    }
    * { margin:0; padding:0; box-sizing:border-box; }
    body {
      background:var(--bg);
      color:var(--text);
      font-family: system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;
      min-height:100vh;
      display:flex; flex-direction:column;
    }
    header {
      text-align:center; padding:18px;
    }
    .banner {
      max-width:480px; width:100%;
      border-radius:12px; box-shadow:0 12px 32px var(--shadow);
    }
    .banner-caption {
      margin-top:10px;
      font-size: clamp(1rem, 2.5vw, 1.3rem);
      font-weight:700;
      color: var(--gold);
      text-align:center;
      letter-spacing:.03em;
    }
    .login-card {
      background:var(--panel);
      max-width:400px;
      margin: 20px auto;
      padding:30px 25px;
      border-radius:12px;
      box-shadow:0 6px 18px var(--shadow);
      text-align:center;
    }
    .login-card img {
      max-width:180px;
      margin-bottom:20px;
    }
    .commander-title {
      font-size:1.2rem;
      font-weight:700;
      display:flex;
      align-items:center;
      justify-content:center;
      gap:8px;
      margin-bottom:20px;
      color:var(--text);
    }
    .commander-title .icon { font-size:1.4rem; }
    input {
      width:100%;
      padding:12px;
      margin-bottom:16px;
      border-radius:8px;
      border:1px solid #2b2f3a;
      background:#0f1118;
      color:var(--text);
      font-size:1rem;
    }
    button {
      width:100%;
      padding:14px;
      border:none;
      border-radius:8px;
      background:var(--gold);
      color:#000;
      font-weight:700;
      font-size:1rem;
      cursor:pointer;
      transition:.25s;
    }
    button:hover { background:#ffd700; }
    footer {
      padding:14px;
      text-align:center;
      font-size:.9rem;
      color:var(--muted);
    }
  </style>
<link rel="stylesheet" href="/assets/css/empire-style.css">
<script src="/assets/js/empire-frame.js" defer></script>
<link rel="stylesheet" href="/assets/css/empire-style.css">
<script src="/assets/js/empire-frame.js" defer></script>
</head>
<body>
  <header>
         alt="The Empire Command Banner" 
         class="banner"/>
    <div class="banner-caption">⚔️ The Empire Commander Login ⚔️</div>
  </header>

  <div class="login-card">
    <img src="/assets/brand/empire-logo.png" alt="The Empire Crown Logo"/>
    <div class="commander-title">
      <span class="icon">🗡️</span> Commander Login <span class="icon">🗡️</span>
    </div>
    <form>
      <input type="password" placeholder="Access Key"/>
      <button type="submit">Enter The Empire</button>
    </form>
    <p style="margin-top:12px; font-size:.85rem; color:var(--muted)">
      By entering you agree to Empire terms.
    </p>
  </div>

  <footer>Commanding the Future of Affiliate Marketing</footer>
</body>
</html>
